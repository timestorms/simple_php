<?php
    if (!defined('BASEPATH')) {
        exit('No direct script access allowed');
    }

    require_once(BASEPATH . 'core/Common' . EXT);
    require_once(BASEPATH . 'config/constant' . EXT);

    
?>